To perform a projected DOS (pDOS) plot, only one Conquest calculation is needed.

Once the Conquest calculation has been performed, the post-processing
calculation produces the total DOS for the system along with the projected
DOS for all atoms (in this case).  The reference plots were plotted
using the Jupyter notebook but we also provide Grace files to do this.
