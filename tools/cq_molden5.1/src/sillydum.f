      subroutine oginid(r,adjus,natoms,nat,icol,xsym,ysym,zsym,
     &                  vdwr,cnst,npts1,npts2,iorb,dens)
      implicit double precision (a-h,o-z)
      dimension r(*),xsym(*),ysym(*),zsym(*),vdwr(*)
      dimension nat(*),icol(*),dens(*)

      idum = 0

      return
      end
